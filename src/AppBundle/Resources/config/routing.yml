show_sprint:
    path: /sprints/{id}
    defaults:
        _controller: AppBundle:Sprint:show
    requirements:
        id: \d+
        _method: GET

close_sprint:
    path: /sprints/{id}/close
    defaults:
        _controller: AppBundle:Sprint:close
    requirements:
        id: \d+
        _method: POST
